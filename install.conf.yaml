- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vimrc:
      relink: true
    ~/config/i3/config: i3.config      
    ~/.zshrc:

- create:
    - ~/Downloads
    - ~/.vim/undo-history

- shell:
  - [git submodule update --init --recursive, Installing submodules]
